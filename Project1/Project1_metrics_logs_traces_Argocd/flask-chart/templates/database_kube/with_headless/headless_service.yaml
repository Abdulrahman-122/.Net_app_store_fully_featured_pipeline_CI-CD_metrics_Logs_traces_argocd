apiVersion: v1
kind: Service
metadata:
  name: {{.Release.Name}}-mariadb-headless
  labels:
    app: mariadb
spec:
    ports:
      - port: 3306
        name: mysql
    clusterIP: None
    selector:
      app: mariadb
