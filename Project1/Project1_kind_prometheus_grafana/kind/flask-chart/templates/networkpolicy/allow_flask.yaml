apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: allow-flask
spec:
  podSelector:
    matchLabels:
      app: {{.Release.Name}}
  ingress:
    - ports:
        - protocol: TCP
          port: 5000
  policyTypes:
    - Ingress
