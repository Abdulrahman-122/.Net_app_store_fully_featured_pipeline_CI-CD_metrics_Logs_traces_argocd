apiVersion: v1
kind: Service
metadata:
  name: mariadb
  namespace: my-ns1

spec:
  clusterIP: None   #headless service

  selector:
    app: mariadb

  ports:
    - port: {{.Values.database.port}}
      targetPort: {{.Values.database.targetPort}}
